# Every pull request needs a review from the owner before it can merge. The
# branch rule on main requires a code owner specifically, so this file is the
# thing that makes that rule mean anything.
* @clickspider
